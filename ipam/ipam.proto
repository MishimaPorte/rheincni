syntax = "proto3";

package ipam.v1;

option go_package = "rheincni/ipam/gen/ipamv1;ipamv1";

import "google/protobuf/empty.proto";

message AllocateIPRequest {
  sfixed32 host_eth_index = 1;
}

message IP {
  fixed32 ip = 1;
  fixed32 gw = 2;
}

message IPAMInfo {
  fixed32 subnet = 1;
  fixed32 subnet_prefix = 2;
  fixed32 gw = 3;
}

service IPAMService {
  rpc GetInfo(google.protobuf.Empty) returns (IPAMInfo);
  rpc AllocateIP(AllocateIPRequest) returns (IP);
  rpc DeallocateIP(IP)              returns (google.protobuf.Empty);
}
